# Turbo Utilities

This directory is for out-of-band operator and development utilities.

Process-managed Turbo runtime code lives in `openpilot/tools/turbo` and is
launched by `openpilot/system/manager/process_config.py`.
